### Trie概念

![image-20250405114038799](字典树.assets/image-20250405114038799.png)

- insert(string s):在字典树中插入字符串s
![image-20250405114950527](字典树.assets/image-20250405114950527.png)

- get(prefix pre):返回前缀pre出现的次数

![image-20250405115909919](字典树.assets/image-20250405115909919.png)

- count(string s): 返回s在字典中出现的次数

![image-20250405120912742](字典树.assets/image-20250405120912742.png)


- delete(string s):在字典树中删除字符串s

![image-20250405130044116](字典树.assets/image-20250405130044116.png)

### 模板

```c++
struct Trie{	
    const static int MAXN = 2e5+10; //根据题目数据量调整
    int trie[MAXN][26];//假设字符集全为小写字母
    int cnt = 1; //根节点从1开始，0视作空节点
    int pass[MAXN]; //经过i的路径数
    int end[MAXN]; //以i结尾的单词数。
    
    //插入单词s
    void insert(const string& s){
        int cur = 1;
        pass[cur]++;
        for(int i =0;i < s.size(); i++){
            int c = s[i] - 'a';
            if(!trie[cur][c]){ //没有节点，构建
                trie[cur][c] = ++cnt;
            }
            cur = trie[cur][c];
            pass[cur]++;
        }
        end[cur]++; 
    }
    
//TODO:下面两个函数重叠部分都是相同逻辑，可封装到一个函数中.
    //获取前缀s出现的次数
    int get(const string& s){
        int cur = 1;
        for(int i =0 ;i < s.size(); i++){
            int c = s[i]- 'a';
            if(!trie[cur][c]) return 0; //没有在字典树中
            cur = trie[cur][c];
        }
        return pass[cur];
    }
   
    //获取单词s出现的次数
    int count(const string& s){
        int cur = 1;
        for(int  i =0;i < s.size(); i++){
            int c = s[i] - 'a';
            if(!trie[cur][c]) return 0; //没有在字典树中
            cur = trie[cur][c];
        }
        return end[cur];
    }
    
    //将单词s在字典树中的次数减1
    void delete_(const string& s){
        if(count(s)){ //s在字典树的数量 > 0
            int cur = 1;
            pass[cur]--;
            for(int i =0;i < s.size(); i++){
                int c = s[i] - 'a';
			   if(--pass[trie[cur][c]] == 0){
                   trie[cur][c] = 0; //删除路径
                   return;
               }
                cur = trie[cur][c];
            }
            end[cur]--; //将s的次数减1
        }
    }
};

//注意，由于数组开的空间较大，不要在main函数中声明，要放到全局变量中声明。
```



### 习题练习

- https://www.nowcoder.com/practice/7f8a8553ddbf4eaab749ec988726702b `模板题`
- https://www.luogu.com.cn/problem/P1481 `前缀链的最大长度。这道题字典无重复单词.`
- https://www.luogu.com.cn/problem/solution/P2580 `模板题`
- https://www.nowcoder.com/practice/c552d3b4dfda49ccb883a6371d9a6932 `找前缀数量,数字较大如何做？转化为字符串。` `经典题`
- [依依的瓶中信](https://www.lanqiao.cn/problems/3751/learning/?page=1&first_category_id=1&name=%E4%BE%9D%E4%BE%9D%E7%9A%84%E7%93%B6%E4%B8%AD%E4%BF%A1) `字典中找最长公共前缀`
- [421. Maximum XOR of Two Numbers in an Array](https://leetcode.cn/problems/maximum-xor-of-two-numbers-in-an-array/) `数组中两数的最大异或和.哈希做法很有趣`&#x1F60D;
- [Tk的排列间异或](https://ac.nowcoder.com/acm/contest/107500/F) `求1~n中的最大异或对排列，思维。`
- https://leetcode.cn/problems/word-search-ii/description/ `搜索剪枝`  &#x1F60D;
- https://www.luogu.com.cn/problem/P4551 &#x1F60D;&#x1F60D; `树上异或路径性质:LCA思想,01trie`
- 

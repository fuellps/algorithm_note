[toc]



### 思路

#### 动态规划

$$
\begin{aligned}
&对于子数组最大和,一般定义dfs(i)表示以i结尾的子数组最大和. \\
&分类讨论： \\
&选择和以i-1结尾的子数组拼起来形成新的子数组\\

&或者自己单独作为子数组，不和i-1结尾的子数组拼起来\\

&即dfs(i)= max(dfs(i-1),0)+nums[i]\\
\end{aligned}
$$

> 注意:这个状态方程只依赖于前一个状态,每个状态只会访问一次,因此不会由重复访问的情况.因此只需普通递归就行,不需要开记忆化数组.

```java
//递归:下面代码求整个数组的最大子数组和
int ans = Integer.MIN_VALUE; //整个数组的最大子数组和
//dfs(i)表示以i结尾的最大子段和。
int dfs(int i,int[] nums){
    if(i==0) return nums[0]; //或者 	if(i < 0) return 0; 
    int res = Math.max(dfs(i-1),0) + nums[i];
    ans = Math.max(ans, res);
    return res;
}
dfs(n-1);
return ans;
```

```java
//动态规划代码
int dp = new int[nums.length];
int ans = dp[0] = nums[0];
for(int i = 1;i < nums.length; i++){
    dp[i] = Math.max(dp[i-1],0) + nums[i];
    ans = Math.max(ans,dp[i]);
}
return ans;
```

>  空间优化:每个状态只依赖于前一个状态,因此只需一个变量来滚动更新即可.

```java
int ans,f0;
ans = f0 = nums[0];
for(int i = 1; i < nums.length; i++){
    f0 = Math.max(f0,0) + nums[i];
    ans = Math.max(ans,f0);
}
return ans;
```

#### 前缀和

$$
\begin{aligned}
&定义s[i]表示nums数组前i个数的元素和。特别的，规定s[0] = 0 \\
&则有\sum^{i}_{j=0}s[i+1] = s[i] + nums[i](nums数组下标从0开始) \\
&对于 \sum^{r}_{l=i}nums[i] = s[r+1] - s[l].(nums数组下标从0开始) \\
&想要让子数组和最大，可以枚举子数组右端点，同时维护前缀最小值。
\end{aligned}
$$

```c++
int get_submax(){
    int s = 0,mns = 0;
    ans = -0x3f3f3f3f;
    for(int i =0;i < n; i++){
        s += nums[i];
        ans = max(ans, s - mns);
        mns = min(mns,s);
    }
    return ans;
}
```

### 变形题

-  $如果改成返回和最大的子数组，应当如何修改代码？$

>  维护得到最大答案的子数组两个端点下标

- 如果子数组的长度有下界，要怎么做？

> 同样维护前缀最小值.其中前缀最小值左端点满足$j \in [1,i-m]$.

见：[P1404 平均数](https://www.luogu.com.cn/problem/P1404)

- 如果子数组的长度有上界，要怎么做？(子数组长度不超过m)

> 用前缀和定义来做.枚举前缀和数组右端点$i$,用单调队列维护下标$j \in [i-m,i-1]$的前缀最小值.

https://ac.nowcoder.com/acm/contest/1006/D

- 最大两段子段和

[P2642 双子序列最大和](https://www.luogu.com.cn/problem/P2642) 要求两个子段不能重叠 &#x1F60D;&#x1F60D;

- 扩展到二维(最大子矩阵和)

https://www.luogu.com.cn/problem/P1719 简化版

https://leetcode.cn/problems/max-submatrix-lcci/ 



### 习题练习

> 做题时注意要思考清楚边界

-  [2606. 找到最大开销的子字符串](https://leetcode.cn/problems/find-the-substring-with-maximum-cost/) 1422
-  [1749. 任意子数组和的绝对值的最大值](https://leetcode.cn/problems/maximum-absolute-sum-of-any-subarray/) 1542 最大的 $子段和的绝对值$ &#x1F60D;
-  [1191. K 次串联后最大子数组之和](https://leetcode.cn/problems/k-concatenation-maximum-sum/) 1748 &#x1F60D;
-  [918. 环形子数组的最大和](https://leetcode.cn/problems/maximum-sum-circular-subarray/)  1777 环形子数组最大和 &#x1F60D;
-  [2321. 拼接数组的最大分数](https://leetcode.cn/problems/maximum-score-of-spliced-array/) 1791 
- 

### 参考资料

- https://www.luogu.com.cn/article/0e229ar6

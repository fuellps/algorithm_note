[toc]

# 模板汇总

## 一:01背包

### 最多为target模型

> 前言:01背包问题是指有n个物品,第i个物品体积为w[i],价值为v[i],每个物品只能选1次,能装入体积为V的背包的最大价值是多少?

$$
\begin{aligned}
&状态表示:定义dp[i][j]表示从前i个物品中选，总体积不超过j所能得到的最大价值 \\
&可得状态转移方程为: dp[i][j] = max(dp[i-1][j],dp[i-1][j-v[i]]) +w[i] \quad(j \ge v[i]) \\
&边界条件:i<0时,没有数可以选,体积不超过j的最大价值为0. \\
&初始化: \sum^{m}_{j=0}dp[0][j] = 0,表示没有物品时总体积不超过j的最大价值为0 \\
&由于上述方程只依赖于i-1这一行，因此可以利用滚动数组进行优化，\\
&进一步，由于j-v[i]\le j,因此可以只用一维数组进行滚动更新即可。 \\
\end{aligned}
$$

> 一维数组01背包框架：

```java
for(int x : nums){ //枚举物品
   for(int j = V; j >= x; j--){ //枚举体积
       dp[j] = f(dp[j],dp[j-x]); //f代表题目中的要求
   } 
}
```

### 恰好为target模型

$$
\begin{aligned}
&状态表示:dfs(i,j)表示从下标0到下标i中选择,得到的子序列和恰好为j的最大价值(具体题目不同定义) \\
&可得状态转移方程为: dp[i][j] = max(dp[i-1][j],dp[i-1][j-v[i]]) +w[i] \quad(j \ge v[i]) \\
&边界条件:i<0时,如果j==0返回0,状态合法.否则返回-inf,表示状态不合法. \\
&初始化:dp[0][0]=0, \sum^{m}_{j=0}dp[1][j] = -inf,表示只有从前0个物品中选只有体积为0的状态合法 \\
&由于上述方程只依赖于i-1这一行，因此可以利用滚动数组进行优化，\\
&进一步，由于j-v[i]\le j,因此可以只用一维数组进行滚动更新即可。 \\
\end{aligned}
$$



```java
	int s  = 0;
	for(int x : nums){
        s += x;
        //优化:只枚举前缀和目标和中的最小值
        for(int c = Math.min(s,target); c >= 0; c--){
            dp[c] = f(dp[c], dp[c-x]);
        }
    }
```



### 至少为target模型

$$
\begin{aligned}
&状态表示:定义dp[i][j]表示从前i个数中选,组合成的元素和至少为j的方案数(具体题目不同定义) \\
&可得状态转移方程为: dp[i][j] = dp[i-1][j] + dp[i-1][max(j-v[i],0)] \\
&边界条件:i<0时,如果j \le 0 返回1,找到一种方案.否则返回0,表示没有找到方案. \\
&注意上述max(j-v[i],0),这必须保证原始价值没有非负数,则至少为负数的方案数等价于至少为0的方案数\\
&进一步，由于j-v[i]\le j,因此可以只用一维数组进行滚动更新即可。 \\
\end{aligned}
$$



```java
//求和至少为target的方案数
dp[0] = 1;
for(int x : nums[i]){
    //为什么这道题背包容量可以到达0而不是到达x? 这是因为dp数组的定义是`至少`,而不是`恰好`,至少为x也满足至少为0,因此0是1个合法的状态,就应该被计算得到.
    //背包的容量可以到0
    for(int c = t; c >= 0; c--){
        dp[c] += dp[Math.max(c-x,0)]; 
    }
}
```

### 常数优化

对于01背包问题，一个常数优化的做法是维护物品体积的前缀和 $s$,遍历时只需遍历背包容量为 $min(s,V)$即可。这是因为前面的物品最多只能凑出s，如果求最多价值和(s,V]这段区间的内容和s是一致的，如果是求恰好那么都是不满足的状态。

> 注意：如果用这种写法求标准的01背包问题，最后需要遍历dp数组求最大值。因为`dp[n][m]`不一定会被遍历到。

## 二:完全背包

> 有n种物品,第i个物品的体积为w[i],价值为v[i],每种物品可以选择无限个,求装入背包容量为W的最大价值.

$$
\begin{aligned}
&类似于01背包，我们可以定义dp[i,j]表示从前i个物品中选,总体积不超过W的最大价值,\\
&枚举第i个物品选几个，则dp[i,j] =max(dp[i-1,j],dp[i-1,j-\sum_{k=1}^{x}k*v_i]+k*w_i) \\
&x为满足 x*v_i \le j的最大正整数。\\
\end{aligned}
$$



$定义dp[i][j]表示前i个物品中选,体积不超过W的最大价值.$

$子问题?不选第i个物品,从dp[i-1][j]转移而来$

$j >= w[i],可以选第i个物品,从dp[i][j-w[i]]转移而来$

$当i < 0时,没有物品可以选,此时背包为空,返回0$

```java
//代码示例
for(int i = 0;i < n; i++){
    for(int j = w[i]; j <= W; j++){
        dp[i+1][j] = dp[i][j];
        if(j >= w[i]){
            dp[i+1][j] = Math.max(dp[i+1][j],dp[i+1][j-w[i]] + v[i]);
        }
    }
}

//空间优化:
for(int i = 0;i < n; i++){
    for(int c = w[i]; c <= W; c++){
        dp[c] = max(dp[c], dp[c-w[i]] + v[i]);
    }
}

```



## 三:多重背包

> 有n种物品,每种物品能选$cnt_i$个,每种物品的体积为$v_i$,价值为$w_i$,在体积不超过m的情况下,求所能得到的最大价值

### 朴素版本

```c++
int multipleBackpack(){
    for(int i  =0;i < n; i++){
        for(int j = m; j >= 0; j--){
            for(int k = 0;k*v[i] <= j && k <= cnt[i];k++){
                dp[j] = max(dp[j],dp[j-k*v[i]] + k*w[i]);
            }
        }
    }
    return dp[m];
}
```

### 二进制分组优化

```c++
int multipleBackpack{
    cin >> n >> m;
    for(int i = 0,x,volume,weight; i <n; i++){
        //读入每个物品的体积，价值，次数。
        cin >>  volume >> weight >> x;
        for(int k = 1;k <= x; k<<=1){
            v[cnt] = k*volume,w[cnt] = k*weight;
            x -= k;
            cnt++;
        }
        if(x > 0){
            v[cnt] = x*volume,w[cnt] = x*weight;
            cnt++;
        } 
    }
    //分解后就可视为01背包问题。
    for(int i  =0;i < cnt; i++){
        for(int j = m;j >= v[i]; j--){
            dp[j] = max(dp[j], dp[j-v[i]] + w[i]);
        }
    }
    return dp[m]
}
```



## 四:分组背包

$$
分组背包有两种常见类型.\\每个组必须选1个.若进行空间压缩,则在计算当前背包容量前\\需要将当前枚举到的背包容量状态值置为初始态.对于无法到达的状态,也需要置为初始态.\\ \\
每个组至多选1个.若进行空间压缩,则不需要进行置为初始态.\\因为前1个分组不选的状态不会影响当前"每个组至多选1个"的状态方程
$$




> 有n个袋子,每个袋子有m个物品,第i个袋子的第j个物品的价值为v[j],体积为w[i],在容量不超过W的背包中,返回你能获得的最大价值.
>
> !!!注意这个题意是每个袋子都必须选1个.不能从什么都不选的状态转移而来.



$定义dfs(i,j)表示在前i个袋子中选(1个物品!!!),体积不超过j所能得到的最大价值$

$子问题?枚举袋子的物品体积为x,价值为v,则从dfs(i-1,j-v)转移而来$

```java
int[][] dp = new int[n+1][t+1];//多插入1个状态
//初始化:体积不超过0的最大价值为0,因此默认值即为初始化值.
for(int i = 0;i < n; i++){
    for(int j = 0; j <= t; j++){
        for(int x : w){
            if(j >= x){
                dp[i+1][j] = Math.max(dp[i+1][j],dp[i][j-x] + v[i]);
            }
        }
    }
}


//空间优化:
for(int i = 0;i < n; i++){
    //注意这里体积一定要枚举到0,因为不能从什么都不选的状态转移而来
    for(int j = t; j >= 0; j--){
        dp[j] = 0; //当前价值得由前面所决定. !!!每个分组必须选一个问题,则必须置0
        for(int x : w){
            if(j >= x){
                dp[j] = M
            }
        }
    }
}
```

## 五:混合背包

对于混合01背包,完全背包,多重背包的问题,只需要判断当前枚举到的物品属于哪一类型,按照对应的类型进行状态方程的转移即可.





# 习题练习

### 01背包

#### 01背包模板题

[P1048 [NOIP 2005 普及组] 采药](https://www.luogu.com.cn/problem/P1048)

- [P1049 [NOIP 2001 普及组] 装箱问题](https://www.luogu.com.cn/problem/P1049)
- [P1164 小A点菜](https://www.luogu.com.cn/problem/P1164) 求方案数
- [P1510 精卫填海](https://www.luogu.com.cn/problem/P1510) 稍微变形
- [P1926 小书童——刷题大军](https://www.luogu.com.cn/problem/P1926) 稍微变形
- [P1802 5 倍经验日](https://www.luogu.com.cn/problem/P1802) 稍微变形

##### 01背包变形题

- [P8742 [蓝桥杯 2021 省 AB] 砝码称重](https://www.luogu.com.cn/problem/P8742) &#x1F60D;
- [P2392 kkksc03考前临时抱佛脚](https://www.luogu.com.cn/problem/P2392)&#x1F60D;&#x1F60D;
- [P1466 [USACO2.2] 集合 Subset Sums](https://www.luogu.com.cn/problem/P1466) 也可爆搜
- [P2370 yyy2015c01 的 U 盘](https://www.luogu.com.cn/problem/P2370) &#x1F60D;&#x1F60D;
- [CF294B Shaass and Bookshelf](https://codeforces.com/contest/294) &#x1F60D;&#x1F60D;&#x1F60D; 注意细节
-  [3082. 求出所有子序列的能量和](https://leetcode.cn/problems/find-the-sum-of-the-power-of-all-subsequences/) 2242 `子序列的子序列`

#### 巧好装满类型

-  [2915. 和为目标值的最长子序列的长度](https://leetcode.cn/problems/length-of-the-longest-subsequence-that-sums-to-target/) 1659
-  [416. 分割等和子集](https://leetcode.cn/problems/partition-equal-subset-sum/)
-  [494. 目标和](https://leetcode.cn/problems/target-sum/)
-  [2787. 将一个数字表示成幂的和的方案数](https://leetcode.cn/problems/ways-to-express-an-integer-as-sum-of-powers/) 1818
-  [3180. 执行操作可获得的最大总奖励 I](https://leetcode.cn/problems/maximum-total-reward-using-operations-i/) 1849
-  [3181. 执行操作可获得的最大总奖励 II](https://leetcode.cn/problems/maximum-total-reward-using-operations-ii/) 2688 bitset 优化
-   [1049. 最后一块石头的重量 II](https://leetcode.cn/problems/last-stone-weight-ii/) 2092
-   [1774. 最接近目标价格的甜点成本 ](https://leetcode.cn/problems/closest-dessert-cost/)`加深对背包的理解` 
-    [956. 最高的广告牌](https://leetcode.cn/problems/tallest-billboard/) 2381   `转化` 从数组中找长度最长的两个不相交且和相等的集合

#### 至少装满类型

-  [879. 盈利计划](https://leetcode.cn/problems/profitable-schemes/) 2204  &#x1F60D;&#x1F60D;&#x1F60D;  `加深对DP边界和状态定义的理解`

### 多维费用背包

[474. 一和零](https://leetcode.cn/problems/ones-and-zeroes/)

[会员] [2431. 最大限度地提高购买水果的口味](https://leetcode.cn/problems/maximize-total-tastiness-of-purchased-fruits/) 

[3082. 求出所有子序列的能量和](https://leetcode.cn/problems/find-the-sum-of-the-power-of-all-subsequences/) (第一遇见) :red_circle::red_circle:



##### 判断能否凑成某个数

[416. 分割等和子集](https://leetcode.cn/problems/partition-equal-subset-sum/)

[3180. 执行操作可获得的最大总奖励 I](https://leetcode.cn/problems/maximum-total-reward-using-operations-i/)

[1774. 最接近目标价格的甜点成本](https://leetcode.cn/problems/closest-dessert-cost/)(有比较繁琐的实现和一个相对比较简单的实现)



##### 记数型01背包

https://www.luogu.com.cn/problem/P1164 基础

https://atcoder.jp/contests/abc044/tasks/arc060_a atc.C



- [1155. 掷骰子等于目标和的方法数](https://leetcode.cn/problems/number-of-dice-rolls-with-target-sum/) 1654
- [1981. 最小化目标值与所选元素的差](https://leetcode.cn/problems/minimize-the-difference-between-target-and-chosen-elements/) 2010
- [2218. 从栈中取出 K 个硬币的最大面值和](https://leetcode.cn/problems/maximum-value-of-k-coins-from-piles/) 2158

每一轮选出1个最小/最大值,循环n-1轮即可得到n-1个最小/最大值,此时数组有序.

```c++
//使数组非递减序
for(int i = 0;i + 1 < n; i++){
    int t = i;
    //找到最小值
    for(int j = i + 1; j < n; j++){
        if(nums[j] < nums[t]){ 
            t = j;
        }
    }
    swap(nums[t], nums[i]);
}
```

